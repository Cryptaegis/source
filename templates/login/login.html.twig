{# templates/login/index.html.twig #}
{% extends 'base.html.twig' %}

{# ... #}

{% block body %}
    {% if error %}
        <div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
    {% endif %}

    <form action="{{ path('app_login') }}" method="post">
        <label for="username">Email:</label>
        <input type="text" id="username" name="_username" value="{{form.email}}">

        <label for="password">Password:</label>
        <input type="password" id="password" name="_password">
        <input type="hidden" name="{{ path('membre')}}" value="/account"> 

        <button type="submit">login</button>
    </form>
    Vous n'avez pas de compte ? <a href="{{ path('inscription_agent') }}">Opérateur</a> ou
    <a href="{{ path('inscription_agent') }}">Agent</a>
{% endblock %}
